#include <stdio.h>
#include "ft_abs.h"

int     main(void)
{
        int     num1 = -1;
        int     num2 = 0;
        int     num3 = 1;

        printf("ABS(%d) = %d\n", num1, ABS(num1));
        printf("ABS(%d) = %d\n", num2, ABS(num2));
        printf("ABS(%d) = %d\n", num3, ABS(num3));
        return (0);
}
